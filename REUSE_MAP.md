# 代码复用说明

来源是用户已有的 `knowforge-rag-platform` 本地源码。本仓库按实际文件保留通用实现，不声明每个模块都是为物流业务从零编写，也不将代码整理日期当作历史开发经历。

| 模块 | 复用方式 | 物流适配点 |
| --- | --- | --- |
| FAQ/文档混合检索、重排与引用 | 保留通用实现 | 独立 Milvus 集合与 source 白名单 |
| 入库、切分、版本与质量检查 | 保留通用实现 | 物流 FAQ 和合成政策入库，通过门禁后激活 |
| 数据范围与缓存 | 保留通用实现 | 真实部署需要可信的租户身份入口与权限集成 |
| 场景注册器与 source 匹配 | 保留通用实现 | 新增 tracking / claims / returns 三类配置 |
| 页面和 FastAPI 服务 | 复用已有界面与接口 | API 标题与示例环境切换到 KnowLoop |
| 离线评测 | 保留通用评测框架 | 新增 4 条物流输入，尚未取得端到端指标 |

新增内容包括 `scenarios/logistics_after_sales/`、`eval_sets/logistics_after_sales.json` 和配置回归测试。它们只表达合成政策知识，不包含查件、赔付执行、地址修改等真实业务动作。

可继续复用现有核心框架扩展真实场景，但需取得业务资料授权、补充物流问法标注与坏例、验证租户隔离，并将事实性回答限制在当前有效知识版本。当前仓库不包含生产部署证据、真实客户数据和效果指标。
